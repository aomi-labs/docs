---
title: "API Reference"
sidebarTitle: "Overview"
description: "The versioned Aomi HTTP API: resources, authentication, request conventions, and the interactive endpoint reference."
---

The Aomi API turns natural-language intent and deterministic operations into verified on-chain execution. Two versioned resources cover the whole surface:

| Resource | Owns | Use it for |
| --- | --- | --- |
| [`/v1/agent`](/api-reference/agent) | Conversation state | Stateful turns, progress events, actions, and sessions |
| [`/v1/pipeline`](/api-reference/pipeline) | Nothing — stateless | Catalog discovery and the build → simulate → commit lifecycle |

Every endpoint on the pages in this tab has an interactive playground. The TypeScript [Client SDK](/integrate/client-sdk) wraps this same API with typed transports, automatic authentication, and schema validation — prefer it when your application is TypeScript.

## Authentication

All protected requests carry a bearer credential:

```bash
-H "Authorization: Bearer $AOMI_ACCESS_TOKEN"
```

Tokens are bound to an exact resource — an Agent token cannot call Pipeline, and REST tokens cannot call MCP. Guest sessions cover only the guest-safe scopes enabled by the environment. See the [Authentication reference](/api-reference/authentication) for resource audiences, scopes, and failure statuses, and [Authentication](/integrate/authentication) in Integrate for acquiring tokens (guest, device OAuth, widget).

## Conventions

These apply to every call:

- Keep the origin in environment configuration — production and staging can expose different API versions during a rollout.
- Send and receive JSON unless an endpoint returns a document.
- Send `Idempotency-Key` with every mutation — requests without one are rejected — and reuse one key only for retries of the same logical request.
- Capture `X-Request-Id` from every failed response, and respect `Retry-After` on throttled or deferred requests.
- The API stack serves its contract at `/openapi.json`, but the published version can differ per environment; pin types from the released TypeScript client instead of fetching that document at startup.

---
title: Theming
description: Customize colors, typography, and visual appearance using CSS variables and Tailwind.
---

Aomi components follow shadcn conventions. Since component source files are copied into your project, you have full control over styling.

## CSS Variables

shadcn uses HSL CSS variables for theming. Define them in your global CSS:

```css
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.9%;
    --primary: 0 0% 9%;
    --primary-foreground: 0 0% 98%;
    --muted: 0 0% 96.1%;
    --muted-foreground: 0 0% 45.1%;
    --border: 0 0% 89.8%;
    --ring: 0 0% 3.9%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 0 0% 3.9%;
    --foreground: 0 0% 98%;
    --primary: 0 0% 98%;
    --primary-foreground: 0 0% 9%;
    --muted: 0 0% 14.9%;
    --muted-foreground: 0 0% 63.9%;
    --border: 0 0% 14.9%;
    --ring: 0 0% 83.1%;
  }
}
```

Components reference these variables through Tailwind:

```tsx
<div className="bg-background text-foreground border-border" />
```

## Dark Mode

Components use the `.dark` class for dark mode:

```tsx
import { ThemeProvider } from "next-themes";

<ThemeProvider attribute="class" defaultTheme="system">
  <AomiFrame />
</ThemeProvider>
```

## Common Customizations

### Custom Colors

```css
:root {
  --primary: 221.2 83.2% 53.3%;
  --primary-foreground: 210 40% 98%;
}
```

### Border Radius

```css
:root {
  --radius: 0.75rem;
}
```

### Message Bubbles

Edit `components/assistant-ui/thread.tsx` directly:

```tsx
<div
  className={cn(
    "rounded-lg px-4 py-2",
    isUser ? "bg-primary text-primary-foreground" : "bg-muted",
  )}
>
  {content}
</div>
```

### Sidebar Width

Edit `components/assistant-ui/threadlist-sidebar.tsx`:

```tsx
<Sidebar className="w-64" />
```

### Frame Container

```tsx
<AomiFrame
  className="rounded-xl shadow-lg"
  style={{ height: "calc(100vh - 80px)" }}
/>
```

## The `cn` Utility

```tsx
import { cn } from "@aomi-labs/react";

<div
  className={cn(
    "base-styles",
    isActive && "active-styles",
    className,
  )}
/>
```

## Best Practices

- Edit component source directly rather than using `!important` overrides
- Use CSS variables for colors instead of hardcoded hex values
- Keep Tailwind for layout instead of separate CSS files
- Extend the theme in `tailwind.config` rather than overriding everything inline

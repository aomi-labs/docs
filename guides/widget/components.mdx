---
title: Component Catalog
description: Catalog of all UI components in the Aomi shadcn registry — AomiFrame chat shell, sidebar, thread list, message components, and composer building blocks.
---

All UI components available through the Aomi shadcn registry.

## AomiFrame

The main chat shell with sidebar, thread management, and runtime wiring.

```bash
npx shadcn add https://aomi.dev/r/aomi-frame.json
```

```tsx
import { AomiFrame } from "@/components/aomi-frame";

<AomiFrame height="600px" />
```

## AssistantThread

The chat message list and composer.

```bash
npx shadcn add https://aomi.dev/r/assistant-thread.json
```

```tsx
import { Thread } from "@/components/assistant-ui/thread";

<Thread />
```

## ThreadList

Displays all conversation threads.

```bash
npx shadcn add https://aomi.dev/r/assistant-thread-list.json
```

```tsx
import { ThreadList } from "@/components/assistant-ui/thread-list";

<ThreadList />
```

## ThreadListSidebar

Sidebar wrapper around the thread list with wallet button positioning.

```bash
npx shadcn add https://aomi.dev/r/threadlist-sidebar.json
```

```tsx
import { ThreadListSidebar } from "@/components/assistant-ui/threadlist-sidebar";

<ThreadListSidebar walletPosition="footer" />
```

## Notification

Toast notification component.

```bash
npx shadcn add https://aomi.dev/r/notification.json
```

```tsx
import { NotificationToaster } from "@/components/ui/notification";

<NotificationToaster />
```

## ControlBar Components

### ControlBar

```tsx
import { ControlBar } from "@/components/control-bar";

// Full
<ControlBar />

// Minimal
<ControlBar hideApiKey hideWallet />
```

### ModelSelect

```tsx
import { ModelSelect } from "@/components/control-bar";

<ModelSelect placeholder="Choose model" className="w-[200px]" />
```

### AppSelect

```tsx
import { AppSelect } from "@/components/control-bar";

<AppSelect placeholder="Choose agent" className="w-[200px]" />
```

### ApiKeyInput

```tsx
import { ApiKeyInput } from "@/components/control-bar";

<ApiKeyInput title="API Key" description="Enter your API key to authenticate." />
```

### ConnectButton

```tsx
import { ConnectButton } from "@/components/control-bar";

<ConnectButton connectLabel="Connect Wallet" />
```

### NetworkSelect

```tsx
import { NetworkSelect } from "@/components/control-bar";

<NetworkSelect />
```

## shadcn Primitives

| Component | Description |
| --- | --- |
| `Button` | Clickable button with variants |
| `Input` | Text input field |
| `Label` | Form label |
| `Card` | Content container |
| `Dialog` | Modal dialog |
| `Sheet` | Side panel overlay |
| `Popover` | Floating content panel |
| `Command` | Command palette |
| `Drawer` | Bottom drawer |
| `Sidebar` | Collapsible sidebar |
| `Separator` | Visual divider |
| `Tooltip` | Hover tooltip |

## Dependency Tree

```
aomi-frame
├── assistant-thread
│   ├── markdown-text
│   ├── tooltip-icon-button
│   └── attachment
├── threadlist-sidebar
│   ├── thread-list
│   └── sidebar (shadcn)
├── control-bar/
│   ├── model-select
│   ├── app-select
│   ├── api-key-input
│   ├── wallet-connect
│   └── network-select
├── notification
├── wallet-tx-handler
├── breadcrumb (shadcn)
├── separator (shadcn)
├── popover (shadcn)
└── button (shadcn)
```

npm packages: `@aomi-labs/react`, `@assistant-ui/react`.

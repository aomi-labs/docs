---
title: Widget Installation
description: Install and configure the Aomi chat widget via shadcn.
---

The Aomi widget provides a complete chat interface that you can embed in any Next.js page.

## Install

```bash
npx shadcn add https://aomi.dev/r/aomi-frame.json
```

This installs the `AomiFrame` component, all sub-components, and every dependency in one step.

## Basic Usage

```tsx
import { AomiFrame } from "@/components/aomi-frame";

export default function ChatPage() {
  return (
    <div style={{ height: "100vh" }}>
      <AomiFrame
        backendUrl={process.env.NEXT_PUBLIC_BACKEND_URL!}
        height="100%"
        width="100%"
      />
    </div>
  );
}
```

## Compound Component API

Customize the layout using individual components:

```tsx
<AomiFrame.Root height="100%" backendUrl={process.env.NEXT_PUBLIC_BACKEND_URL!}>
  <AomiFrame.Header
    withControl={true}
    controlBarProps={{
      hideWallet: true,
      hideApiKey: true,
    }}
  />
  <AomiFrame.Composer />
</AomiFrame.Root>
```

## AomiFrame Props

| Prop | Type | Description |
|------|------|-------------|
| `backendUrl` | string | Aomi backend URL |
| `height` | string | Container height (e.g., "100%", "600px") |
| `width` | string | Container width (e.g., "100%", "400px") |

## Control Bar

The ControlBar provides built-in controls for:
- **ModelSelect** — switch AI models at runtime
- **AppSelect** — switch between Aomi Apps
- **ApiKeyInput** — enter scoped API keys
- **ConnectButton** — connect a wallet
- **NetworkSelect** — select blockchain network

## Theming

Customize colors using shadcn CSS variables in your Tailwind configuration. See the [Theming guide](https://aomi.dev/docs/build/ui/widget/theming) for detailed options.

## Next Steps

- [Frontend Setup](/guides/frontend-setup) — choose widget vs headless
- [Headless Library](/guides/headless-library) — build custom UI
- [Quickstart](/getting-started/quickstart) — get started in minutes

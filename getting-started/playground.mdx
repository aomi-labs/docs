---
title: Playground
description: "Interactive playground to configure the AomiFrame chat widget. Preview theming, layout, sidebar, and component options before you install it into your app."
mode: custom
---

<style>{`
  .playground-shell {
    display: flex;
    flex-direction: column;
    height: 100vh;
    overflow: hidden;
    background: var(--background);
  }
  .playground-nav {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 10px 16px;
    border-bottom: 1px solid var(--border);
    background: var(--background);
    color: var(--foreground);
    font-size: 13px;
    flex-shrink: 0;
    flex-wrap: wrap;
  }
  .playground-nav a {
    color: var(--muted-foreground);
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .playground-nav a:hover {
    color: var(--foreground);
  }
  .playground-nav-sep {
    color: var(--border);
  }
  .playground-nav-title {
    font-weight: 600;
    color: var(--foreground);
  }
  .playground-nav-desc {
    color: var(--muted-foreground);
    margin-left: auto;
  }
  .playground-body {
    flex: 1;
    min-height: 0;
    overflow: auto;
    padding: 16px;
    box-sizing: border-box;
  }
  .playground-iframe {
    display: block;
    width: 100%;
    min-width: 1000px;
    height: 100%;
    min-height: 700px;
    border: 1px solid var(--border);
    border-radius: 8px;
  }
  @media (max-width: 1080px) {
    .playground-nav-desc {
      display: none;
    }
  }
  @media (max-width: 600px) {
    .playground-body {
      padding: 8px;
    }
    .playground-iframe {
      border-radius: 4px;
    }
  }
`}</style>

<div className="playground-shell">
  <div className="playground-nav">
    <a href="/getting-started/quickstart">← Back to docs</a>
    <span className="playground-nav-sep">|</span>
    <span className="playground-nav-title">Widget Playground</span>
    <span className="playground-nav-desc">Customize and preview your AomiFrame widget before installing</span>
  </div>
  <div className="playground-body">
    <iframe
      src="https://aomi.dev/embed-playground?embed=true"
      className="playground-iframe"
      title="Aomi Playground Configurator"
      allow="accelerometer; clipboard-write; encrypted-media; gyroscope"
    />
  </div>
</div>
